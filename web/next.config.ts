/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/ownership-tracker',
  assetPrefix: '/ownership-tracker',
}

module.exports = nextConfig
